---
description: Auto-selects best Kaizen method (Gemba Walk, Value Stream, or Muda) for target
argument-hint: 分析対象の説明（オプション）
---

# /analyse

この入口があるCodex設定rootの `skills/analyse/SKILL.md` を全文読み、引数と現在の文脈を渡して、その手順に従う。相対pathは作業directoryではなく同じ設定rootを基準に解決する。
元の本文は `archive/2026-08-31/commands/analyse.md` に保存した。実行時の正本はSkillだけとし、アーカイブは読み込まない。
