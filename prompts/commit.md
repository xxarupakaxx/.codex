---
allowed-tools: Bash(git:*), Bash(python3:*)
argument-hint: [--push]
description: 検証済みの変更だけをcommit
---

# /commit compatibility shim

`skills/commit/SKILL.md`をCodex home基準で全文読み、その契約だけを正本として実行する。
このshimへstage、文案生成、commit、pushの手順を複製しない。
