# 出典情報

アップストリーム URL は `https://github.com/mattpocock/skills/tree/main/skills` です。
コミットは `391a2701dd948f94f56a39f7533f8eea9a859c87` です。
source category は `deprecated` です。
source name は `request-refactor-plan` です。
target name は `planning-refactors` です。
status は `deprecated` です。

ローカル調整として、原典の聞き取り強度、スコープ内外、テスト不足時の確認条件を日本語で明示しました。
