# audit

対象ファイルを読み、視覚上の観測事実をseverity付きで返す。変更はしない。

各findingには、問題名、場所、severity、観測、具体的な修正案、根拠を含める。中央hero、均一カード、隠れた本文、theme drift、fake chrome、overflow、focus欠落を対象にする。実測できないものは推測や数値に置き換えない。

最後にcritical / major / minorの件数を出し、次の工程としてredesignを提案する場合も、自動的に編集へ進まない。
