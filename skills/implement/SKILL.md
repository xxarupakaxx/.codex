---
name: implement
description: 承認済みのspecまたはticketを、合意したtest seamとreviewを保ちながら実装する。`/implement` と明示されたときだけ使う。
disable-model-invocation: true
---

# Implement

これは upstream の正式名を discovery surface に出す user-invoked entry です。

実行規律の正本は `../implementing-work/SKILL.md` です。

起動したら同ファイルを最初から最後まで読み、projectのPhase、TDD、review、commit policyを優先して実行してください。

未決事項が残る場合は実装で埋めず、specまたはhuman gateへ戻します。
