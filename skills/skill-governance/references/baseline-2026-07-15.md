# Skill Estate Baseline — 2026-07-15

この基準値は `inventory` と固定 revision の source catalog から作成した。候補の code、hook、installer は実行していない。

## Local surface

明示した8個の scope baseを再帰走査し、20個の rootへ全件を分類した。登録済みroot外の `SKILL.md` は0件だった。

| Root | Entries |
| --- | ---: |
| `codex` | 145 |
| `claude` | 110 |
| `shared` | 68 |
| `vault-codex` | 138 |
| `vault-claude` | 12 |
| `vault-shared` | 18 |
| `codex-plugin-cache` | 336 |
| `claude-plugin-cache` | 0 |
| `claude-marketplaces` | 32 |
| `codex-scheduled` | 10 |
| `codex-claude-compat-scheduled` | 10 |
| `codex-memory-skills` | 2 |
| `vault-codex-scheduled` | 10 |
| `vault-codex-claude-compat-scheduled` | 10 |
| `vault-claude-global` | 109 |
| `vault-claude-global-scheduled` | 10 |
| `vault-shared-ai` | 8 |
| `claude-scheduled` | 15 |
| `vault-codex-mirror` | 139 |
| `vault-claude-mirror` | 109 |
| **Total** | **1291** |

- logical name: 954件。
- logical name collision group: 142件。
- raw name collision group: 166件。
- runtime上で同じprecedenceかつ異内容のcollision: 8件。現在のauditではBLOCKINGになる。
- exact runtime estate lock: 491件。追加、消失、resolved name、name resolution、tree hash、classificationのdriftをBLOCKINGにする。`skill-governance` 自身の `estate.lock.json` と `reputation.lock.json` は自己参照循環を避けるためcontentだけを固定markerへ正規化し、存在、path、modeと、ほかのpackage fileの全内容は引き続き検証する。両control fileの実byteはGit parity、lock validation、deliveryで別途固定する。

除外は実行面ではない履歴・一時領域に限定した。path単位では `~/.codex/.tmp` 677件、`~/.codex/tmp` 0件、`~/.codex/backups` 101件、`~/.claude/backups` 0件である。directory名単位では `node_modules` 内の `SKILL.md` 8件も列挙し、vendored dependencyとして別枠にした。除外理由、件数、実pathは `surface_probe` に残す。

symlinkは72件を記録した。71件は `.venv`、`node_modules`、`tmp` の除外面であり、targetを記録してfollowしない。残る1件はvendor-managed plugin cacheのstale `latest` pointerで、non-runtime allowlistにtargetを固定した。runtime rootの未登録symlinkはBLOCKINGになる。

frontmatterに `name` がないlegacy command-formatは、`codex` と `vault-codex` の計14entryだけを、root・relative path・exact tree hashへbindしたdirectory-name inferenceとして明示した。hashが変わればcollision checkはfail-closedになる。

## Pinned source catalog

- source: 11件。
- `SKILL.md`: 826件。
- frontmatter `name` 抽出: 822件。
- source横断のunique name: 756件。
- source横断の同名group: 25件。
- 最寄りlicense evidenceがないentry: 415件。

offline `catalog` は固定済みevidenceの整合性だけを確認する。各entryは `SKILL.md` blobに加え、そのfileを含むupstream directory全体のGit tree SHAを持つ。完全性の独立証明には `catalog --live` を使い、固定commitのGit treeにある全 `SKILL.md` path / blob SHAとcomplete package tree SHAを照合する。

11 sourceの評判と保守signalは [source-reputation-2026-07-15.md](source-reputation-2026-07-15.md) と独立した `reputation.lock.json` に日付付きで固定した。調査時点では全sourceがnon-archivedで、pinned revisionはdefault branch HEADと一致した。starや公式性はadvisoryに限定し、license gap、credential、network、hook、global process gateを候補単位で審査する。評判snapshotのrefreshだけではregistry-bound trust lockを変更しない。

## Matt Pocock catalog

固定commit `e9fcdf95b402d360f90f1db8d776d5dd450f9234` には40件ある。stable 28件、in-progress 8件、deprecated 4件である。

CodexとClaudeには39件が同名または別名で存在する。未導入はin-progressの `to-questionnaire` だけであり、stableの欠落は0件だった。ただし、存在することはcurrent revisionとの同一性や承認を意味しない。既存33件は `legacy-active` / `evidence-gap` のまま扱う。

SOURCEがない同名importは `design-an-interface`、`ubiquitous-language`、`diagnosing-bugs`、`research`、`tdd`、`grill-me` の6件である。

## Architecture crosswalk

| Route | Origin | Scope | Boundary |
| --- | --- | --- | --- |
| `improving-codebase-architecture` | upstream `improve-codebase-architecture` の別名導入 | ユーザー指定範囲またはrecent hot spotのscoped survey | HTMLで候補を比較する。実装は行わない |
| `improving-architecture` | local synthesis | 選択済みの1〜3 moduleまたは一つの関心事 | Deletion Test、Seam、Localityで改善設計する |
| `software-architecture` | local | 新規systemの境界とDDD判断 | 既存codebaseの改善surveyではない |
| `designing-codebases` | upstream `codebase-design` の別名導入 | Deep Moduleの語彙とinterface設計 | broad surveyや実装許可ではない |

current upstreamのscope-before-scan規則はrouting policyと両platformの上位global guardへ取り込んだ。Skill本文のWrite/Edit、CONTEXT.md、ADR、実装継続指示よりこのread-only guardを優先する。Vault mirror側のSkill本体更新は、gitlink conflictが解消し、再審査が終わるまでHOLDする。

## Current HOLD

Vault parentの `.codex` と `.claude-global` にはunmerged gitlinkがある。`vault-gitlink-conflict` holdが有効な間は、該当rootへのpromotion、update、retire、mirror applyを行わない。
